name: $(BuildID)

trigger:
  branches:
    include:
    - feat/*
    - fix/*
  paths:
    exclude:
    - '*.md'
    - 'backends/*'
    - 'images/*'
    - '.github/*'

pool:
  vmImage: 'ubuntu-latest'

stages:
- template: stages/ci.yaml
